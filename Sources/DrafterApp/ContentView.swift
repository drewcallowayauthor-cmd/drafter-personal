import SwiftUI

/// M0 placeholder — replaced by the three-pane Binder/Editor/Inspector layout (§8.1).
struct ContentView: View {
    var body: some View {
        Text("Drafter")
            .font(.largeTitle)
            .padding()
            .frame(minWidth: 480, minHeight: 320)
    }
}

#Preview {
    ContentView()
}
